interface:
  display_name: "FutureMUD Climate Seeder"
  short_description: "Tune seeded climates and diagnose climate regressions"
  default_prompt: "Use $futuremud-climate-seeder for the requested climate task, using benchmark evidence when calibration changes and focused regression checks for the affected behaviour."
